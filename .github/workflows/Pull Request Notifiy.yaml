name: Pull Request Notify
run-name: send pull request notification
on: [pull_request]
jobs:
  Check_User_Merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checking User Pull Request
        run: echo "Started by: ${{ github.event.pull_request.user.login }}"

      - name: Send Notification to Slack
        run: |
          echo "Sending Slack notification..."
          curl -X POST -H 'Content-type: application/json' --data '{
            "text": "A pull request was opened in the repository *${{ github.repository }}* by *${{ github.actor }}*."
          }' ${{ secrets.SLACK_WEBHOOK_URL }}
